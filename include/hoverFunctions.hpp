#ifndef HOVERFUNCTIONS_H
#define HOVERFUNCTIONS_H

#include "RenderObject.hpp"

//callingObject: Point to object calling function. isHovered: true if hovered, false if not hovered

    //Generic change of sprite int rect
    void simpleSpriteChange(RenderObject* callingObject,bool isHovered);


#endif
